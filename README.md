# Typescript ABI code generator

Usage: 
```shell
npm run build
npm run generate [abiFile] [contractName] [outputFile]
```

You can generate sample ERC20 ABI:
```shell
npm run generate ./test/erc20.json USDT ./erc20.ts
```
